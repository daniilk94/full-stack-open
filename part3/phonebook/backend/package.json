{
  "name": "phonebook_backend",
  "version": "0.0.1",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "dev": "node --watch app.js",
    "build:ui": "shx rm -rf dist && cd ../frontend && npm install && npm run build && cp -r dist ../backend",
    "deploy:full": "shx npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
